import { useEffect, useState } from "react";
import Weapon from "./Weapon";


export default function DraftBox() {
    useEffect(() => {
        // get 10 random weapons from API or whatever
        
    },[])
    
  return (
    // map over array of items and return a <Weapon> for each item
  )
}
